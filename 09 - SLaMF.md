# Ошибки логирования и сбои мониторинга (Security Logging and Monitoring Failures)

### Цель работы

Проанализировать механизмы логирования и мониторинга в веб-приложении на предмет их эффективности при обнаружении атак. Выявить недостатки в регистрации событий безопасности, отсутствие алертов и реакций на подозрительную активность, а также оценить возможность длительного нахождения злоумышленника в системе без обнаружения.

## Ход выполнения

### 1) Разведка

Для анализа был выбран модуль Command Injection в DVWA. Данный модуль позволяет выполнять системные команды через уязвимый параметр, что оставляет следы в логах веб-сервера.
Для доступа к логам был использован запущенный Docker-контейнер с DVWA. Логи Apache внутри контейнера находились по стандартному пути:

<img width="556" height="175" alt="изображение" src="https://github.com/user-attachments/assets/9ec45c7c-47a7-4094-baaa-3368e1b5b3da" />

В логах на данный момент были зафиксированы лишь переходы пользователя по разным страницам сайта:

<img width="900" height="325" alt="изображение" src="https://github.com/user-attachments/assets/2459a425-6ec8-48f9-9502-cbe16a86a993" />

### 2) Атака

Для проверки эффективности мониторинга была проведена атака: в поле ввода IP-адреса был отправлен следующий payload:

```bash
127.0.0.1 && cat /etc/passwd
```

Сервер успешно выполнил команду и вернул содержимое файла /etc/passwd:

<img width="660" height="526" alt="изображение" src="https://github.com/user-attachments/assets/3a6dca61-9bd3-498c-bc7e-c5e42555d2c9" />

### 3) Эксплуатация

Сразу после проведения атаки были проанализированы логи Apache:

<img width="908" height="204" alt="изображение" src="https://github.com/user-attachments/assets/63ca6ac8-005b-499c-8f6f-9173c77b88ed" />

В момент атаки (12:49:17) был отправлен POST-запрос к `/vulnerabilities/exec/` с вредоносным payload `127.0.0.1 && cat /etc/passwd`. Однако анализ логов показывает критическую неполноту мониторинга:

```log
172.17.0.1 - - [11/Mar/2026:12:49:17 +0000] "POST /vulnerabilities/exec/ HTTP/1.1" 200 2256...
```

В логе зафиксирован только факт POST-запроса, но отсутствует информация о переданных параметрах. Это означает, что администратор, анализируя логи, может увидеть сам факт обращения к уязвимому эндпоинту, но не узнает, была ли это реальная атака и какой payload использовался.

В файле `/var/log/apache2/error.log` также нет каких-либо записей, указывающих на подозрительную активность:

<img width="909" height="140" alt="изображение" src="https://github.com/user-attachments/assets/006d6b49-fe26-4586-815e-82afa2e4878b" />

## Выводы о защищенности

В результате анализа выявлено, что система ведет базовое логирование HTTP-запросов, однако полностью отсутствуют механизмы мониторинга и реагирования на инциденты безопасности. Проведенная атака зафиксирована в логах лишь как обычный POST-запрос без сохранения переданных параметров, что делает невозможным расследование инцидента. В логах ошибок также отсутствуют записи о подозрительной активности, система не генерирует алерты, не блокирует атакующего и не уведомляет администратора. 

Данная проблема классифицируется как:
- **CWE-778: Insufficient Logging** - недостаточное логирование.
- **CWE-223: Omission of Security-relevant Information** - пропуск важной информации безопасности.
